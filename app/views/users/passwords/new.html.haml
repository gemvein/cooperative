- add_title :forgot_your_password.l
= bootstrap_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :class => 'form form-horizontal' }) do |f|
  = f.email_field :email, :autofocus => true
  .controls= f.submit :send_me_reset_password_instructions.l
= render "devise/shared/links"
