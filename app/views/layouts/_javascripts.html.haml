= javascript_include_tag 'cooperative'
= javascript_include_tag 'application'
%script{:type => 'text/javascript'}
  $('.spinner').spin('small');
  $('#modal').on('hidden', modal_dismiss);
  $('#modal').on('show', modal_show);
  $(function() {$('form[data-remote]').each(cooperative_initialize_remote_form).on('load', cooperative_initialize_remote_form);});
= yield :end_javascript
