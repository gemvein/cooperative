-unless images.empty?
  %ul.thumbnails
    -images.each_with_index do |image, index|
      %li.span1= link_to image_tag(image, :class => 'thumbnail'), '#', :class => 'shareable_image', :id => 'image_' + index.to_s
