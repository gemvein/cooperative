prefix = '<%= @commentable.class.name.downcase %>_<%= @commentable.id.to_s %>'
<% if !@comment.new_record? %>
    $('#' + prefix + '_comments_div').append('<%= escape_javascript(render @comment) %>');
    $('#' + prefix + '_comment_body').val('');
<% else %>
    $('#' + prefix + '_comments_div').append("<%= escape_javascript(error_messages @comment)%>");
<% end %>